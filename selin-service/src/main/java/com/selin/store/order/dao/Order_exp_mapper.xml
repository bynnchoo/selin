<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.selin.store.order.dao">
	<sql id="columnsAs">
		t1.id, t1.order_num, t1.create_date, t1.cus_id, t1.cus_num, t1.cus_name, t1.amount, t1.current_status, t1.current_event, t1.pay_status, t1.finish_time, t1.ex_date, t1.remark, t1.receive_address_id, t1.invoice_type, t1.invoice_id
	</sql>
	
	<select id="selectOrderPage" resultType="com.selin.store.order.entity.OrderVo">
		select
		<include refid="columnsAs"/>
		from
		w_order t1
		join
		(SELECT
		id
		from
		w_order
		where 1=1
		<include refid="conds" />
		order by id desc
		limit #{firstrownum}, #{limit}) t2
		where t1.id = t2.id
	</select>
	
	<select id="selectOrderCount" resultType="java.lang.Long">
		select
		count(id)
		from w_order
		where 1=1
		<include refid="conds" />
	</select>
</mapper>